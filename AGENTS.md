# Repository instructions

## Debug documentation

- Put all transient debugging notes, hardware-test reports, benchmark evidence, logs, and generated diagnostic data under `.agents/docs/`.
- Never add those files to the public `docs/` tree or commit them; `.agents/docs/` is intentionally ignored by Git.
- Keep `docs/` limited to durable user-facing or maintainer-facing documentation such as architecture and release instructions.
