# Changelog

## 1.1.0 — 2026-09-18

实时数据路径现统一使用 Rust 数据核心，不再提供 TypeScript 原始采样回退模式。

### 主要变更

- Rust 独占 CKIT 解码、最新值、派生表达式、历史索引、FFT、显示聚合与实时 CSV 写入。
- TypeScript 仅负责进程生命周期、配置、订阅、会话校验以及向 Webview 转发有界结果。
- Webview 仅请求并绘制 Rust 生成的像素级聚合帧，不再保存原始样本或执行 FFT。
- 缓存稳定采样订阅的 Rust 读计划、解码映射和工作缓冲区；相邻普通 RAM 变量按 32 字节间隔合并读取，并将单个上层读块限制为 4 KiB，由 probe-rs 按 MEM-AP 边界切分，减少重复调用和分配。
- 基于 probe-rs 0.31.0 的受控 vendor fork 合并 CMSIS-DAP AP 配置与小块读写事务，并将分散 32 位地址的 TAR/DRW 对按探针报文容量批量发送；其他探针及大于 32 位的地址保留逐地址回退。
- 高请求率采样不再等待 Windows 粗粒度周期定时器；命令队列仍保持优先级，自适应批预算提升至 32 ms 以摊薄 probe-rs Core 获取、事件和编码开销。
- 移除 `cortexKit.nativeDataEngine.enabled` 功能开关；匹配版本的 Rust 后端成为必需组件。
- 删除 TypeScript 原始批次筛选、表达式求值、CSV 解析/抽稀以及旧 RTOS 内存解析旁路；TypeScript 仅保留 UI 与控制面。
- 删除已被 native-data 核心替代的旧 Rust FFT、旧环形队列及其 `crossbeam-queue` 依赖。

## 0.1.13 — 2026-09-14

本版本完善 C++ 全局 / 静态对象及对象指针的查看、成员选择和绘图链路。

### 新增

- 支持展开全局对象指针并单独监视、绘制或修改其 pointee 成员；采样时动态解引用当前 32 位 Cortex-M 指针，空指针返回不可用值且不影响其他通道。

### 修复与改进

- 修复 C++ 全局 / 静态 `instance` 的声明引用解析，去除对应的不可展开符号副本，并显示所属类 / 函数以区分同名对象。
- 变量树使用稳定节点 ID，避免同名对象及数值刷新影响展开；Plot 表达式支持 `::` 限定名和结构体数组成员，保留已有变量选择 ID。
- 原生调试 Statics / Watch 支持对象和数组逐层展开；优先读取已展开对象，避免被大量全局标量耗尽读取配额。
- 从变量树、搜索、Live Watch 或 Plot 选择结构体 / 数组时改为勾选具体内部标量，不再默认将整个对象的所有成员一起加入。

### 验证范围

Windows x64；Horco CMSIS-DAP 与 STM32H723VG（`wbr_2026`），50 MHz attach 模式下通过发布包后端同时采样 `debug_supercap` 的 4 个 pointee 成员，2 秒内每成员 271 个样本，实测约 135.38 S/s，未发现丢样或读取错误；暂停、继续和 Plot-only 保护流程通过。

## 0.1.12 — 2026-09-12

本次发布准备版本汇总了 0.1.5 之后的本地迭代。

### 新增

- 手动选择 ST-Link 或 DAPLink / CMSIS-DAP，识别设备并按 selector 绑定后连接。
- 独立的探针 / 采样配置向导，显式选择协议、时钟、连接方式及最大吞吐请求。
- Plot 可选 1–600 秒时间窗口，支持文件夹设置；结束会话后可选择单个或多个图表合并导出 PNG。
- 中文插件使用指南、参考项目致谢、MIT License 和发布流程。

### 修复与改进

- 采样批次按实测读取耗时调整，降低高请求频率下暂停命令的等待时间。
- Sample 持续后台录制；隐藏或关闭视图不停止，临时数据错误后可继续同一 CSV。
- Plot 保留结束会话后的数据，并恢复面板隐藏期间收到的数据；缓存有时间及数值数量上限。
- 修复 `cortexKit.historySeconds` 写入 Folder Settings 时的资源作用域错误。

### 验证范围

Windows x64；ST-Link 与 STM32H723VG，以及 Horco CMSIS-DAP 的连接、采样、暂停 / 继续。请求 100000 S/s 不代表实测达到该速度；DAPLink 烧录及更多设备组合仍待验证。

## 0.1.5

已有功能包括实时变量与 Plot / FFT、CSV 记录、独立 Live Watch 速率、CMake 构建快捷键和独立烧录。详细开发记录见 Git 历史及 `docs/`。
